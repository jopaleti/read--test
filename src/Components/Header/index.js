// Components
import Navbar from "../navbar";
import Hero from "./hero";

const Header = () => {
  return (
    <header id="hero">
      <Navbar />
      <Hero />
    </header>
  );
};

export default Header;
